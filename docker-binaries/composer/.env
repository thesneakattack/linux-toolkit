# docker-binaries/composer/.env

# PHP+Composer image (use the official Composer image or a custom one)
COMPOSER_IMAGE=composer:2

# Host UID/GID to maintain file permissions
HOST_UID=1000
HOST_GID=1000
